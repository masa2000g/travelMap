rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Anyone may read logs; writes still require authentication.
    match /logs/{document=**} {
      allow read: if true;
      allow write: if request.auth != null;
    }
  }
}
